Thank you for your order, <%= @order.email %>!
=========================

Your order will ship.
Order details:

Order Total: <%= number_to_currency(@order.total_cents / 100.0) %>
<% @order.line_items.each do |line_item| %>
  - <%= line_item.quantity %> x <%= line_item.product.name %>
<% end %>
