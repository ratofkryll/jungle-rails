<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for your order, <%= @order.email %>!</h1>
    <p>Your order will ship.</p>
    <h3>Order details:</h3>
    <p>Order Total: <%= number_to_currency(@order.total_cents / 100.0) %>
    <ul>
      <% @order.line_items.each do |line_item| %>
        <li>
          <%= line_item.quantity %> x <%= line_item.product.name %>
        </li>
      <% end %>
    </ul>
  </body>
</html>
